// import { setupMockServer } from './mockJwt';

// // Only run in development environment
// if (process.env.NODE_ENV === 'development') {
//   console.log('Setting up mock API server for development');
//   setupMockServer();
// }

// export default {};